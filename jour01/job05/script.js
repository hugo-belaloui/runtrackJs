function afficherJourSemaines()
{
    let joursSemaines = ["Dimanche", "Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi"];

    for(let i = 0; i < joursSemaines.length; i++)
    {
        console.log(joursSemaines[i]);
    }
}
afficherJourSemaines();